spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/taskmanager}
spring.datasource.username=root
spring.datasource.password=Prince@123

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}

server.port=${SERVER_PORT:8080}

# JWT settings
app.jwt.secret=${APP_JWT_SECRET:change-this-secret-in-prod-please-update-32bytes}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:3600000}

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:64561}
